---
description: Route A — open PRs; merge only when explicitly asked
alwaysApply: true
---

# Git authority (this repo — Route A)

Follow [`AGENTS.md`](AGENTS.md) **Git authority**.

Non-trivial work: branch → commits → push → **open a PR**. Do not merge unless told “merge …” (e.g. merge #12). Fix / ship / land / green CI ≠ merge permission.

Route B products: do not copy this file.
